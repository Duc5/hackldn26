Place your background image here as:

library-bg.jpg

Path:
frontend/public/library-bg.jpg

The UI applies a built-in warm tint and soft overlay so the image sits behind content without overpowering it.
